region      = "eu-central-1"
environment = "dev"
vpc_cidr    = "10.255.250.0/24"

# Sensitive — inject via TF_VAR_* env vars or a Secrets Manager data source at runtime
rds_password         = "REPLACE_ME"
zabbix_admin_password = "REPLACE_ME"
mqtt_password        = "REPLACE_ME"
